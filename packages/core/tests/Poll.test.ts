import { describe, expect, test } from "bun:test"
import { MaciState } from "../src/Maci"

describe("Poll", () => {

    const maciState = new MaciState()

    test("should have set the correct values", () => {})
    test("copy should produce a deep copy", () => {})

})